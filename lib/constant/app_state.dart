enum UserStatusState { initial, loading, success, failed }

enum AuthStatusState { initial, loading, success, failed }

enum HomeStatusState { initial, loading, success, noData, error }

enum DetailStoryStatusState { initial, loading, succes, error }

enum AuthenticatedStatusState { initial, succes, error }
